# To run the code you need to instantiate the environment with
```
Julia < v1.12 (v1.11.5 works for me!)
```
and the compat to load the objects requires
```
JLD2 v0.5.11
EnsembleKalmanProcesses v2.1.0
```
Which are the versions they were saved in